var noms = ["Jean", "Marc", "Pierre", "Paul", "Jacques", "Robert", "Michel", "Alain", "Luc", "Pierre"]; // tableau de 11 éléments of string
var rand = [1, "teste"]; // tableau de 2 éléments of any
console.log(noms[0]);
var arr_integers = new Array(4); // tableau de 4 éléments of number taille fixe
for (var i = 0; i <= arr_integers.length; i++) {
    arr_integers[i] = i * 2;
    console.log(arr_integers[i]);
}
