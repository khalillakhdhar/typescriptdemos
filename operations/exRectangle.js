function rectangle(hauteur, largeur) {
    var surface = hauteur * largeur;
    var perimetre = 2 * (hauteur + largeur);
    console.log("La surface du rectangle est de " + surface + " et son périmètre est de " + perimetre);
}
rectangle(5, 10);
